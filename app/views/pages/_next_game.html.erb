


<div class="col-md-6 table-responsive text-center ">
  <h4><%= dateFormatTwo(@next_game.when) %></h4>

  <table class="table table-striped table-bordered table-hover table-sm">
    <thead>
      <th bgcolor="#f08080"><%= @next_game.teamA.name %></th>
      <th bgcolor="#add8e6"> <%= @next_game.teamB.name %></th>
    </thead>

    <tbody>
      <%# @next_game.teamA.player_team_rs.pluck(:user_id).each do |user_id| %>
      <% player_teamA_relationship = @next_game.teamA.player_team_rs %>
      <% player_teamB_relationship = @next_game.teamB.player_team_rs %>
      <% totalPlayersA = player_teamA_relationship.length %>
      <% totalPlayersB = player_teamB_relationship.length %>

      <% size = totalPlayersA > totalPlayersB ? totalPlayersA : totalPlayersB %>
      <% (0..size).each do |index| %>
        <tr>
          <td><%= User.find(player_teamA_relationship[index].user_id).name if player_teamA_relationship[index] %></td>
          <td><%= User.find(player_teamB_relationship[index].user_id).name if player_teamB_relationship[index] %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>